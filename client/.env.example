# Only needed when the frontend is hosted on a DIFFERENT domain than the backend.
# Leave blank (or omit this file) if Express serves the built React files directly.
# Example: VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
